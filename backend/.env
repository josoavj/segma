# --- Configuration Serveur ---
DEBUG=True
HOST=0.0.0.0
PORT=8000

# --- Configuration IA (SAM 3 & YOLO) ---
# En 2026, SAM 3 utilise des checkpoints spécifiques. 
# 'facebook/sam3' est le modèle standard.
SAM3_MODEL_ID=facebook/sam3
YOLO_MODEL=yolov8n.pt

# Important : Si tu as un GPU, mets 'cuda'. SAM 3 est lourd pour un CPU seul.
DEVICE=cuda

# --- Gestion des Fichiers ---
# Limite à 50MB (52428800 octets). 
# C'est suffisant pour des images haute résolution.
MAX_FILE_SIZE=52428800
UPLOAD_DIR=./data/uploads
OUTPUT_DIR=./data/masks

# --- Sécurité & CORS ---
# Ajoute l'IP de ton téléphone/émulateur si tu testes sur mobile
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:8080,*

# --- Format de sortie (Contrainte Client) ---
# Choix : 'png' (recommandé pour Flutter) ou 'bin'
MASK_FORMAT=bin