# Configuration Backend SEGMA - Fichier example
# Copiez ce fichier en .env et adaptez les valeurs selon votre configuration

# ============================================
# SERVER CONFIGURATION
# ============================================

# Mode debug (False en production)
DEBUG=False

# Adresse IP d'écoute
HOST=0.0.0.0

# Port d'écoute
PORT=8000

# ============================================
# SAM MODEL CONFIGURATION
# ============================================

# Type de modèle SAM à utiliser
# Options: vit_b (défaut, rapide, ~375MB), vit_l (~1.2GB), vit_h (~2.6GB)
SAM_MODEL_TYPE=vit_b

# Dispositif à utiliser pour l'inférence
# 'cpu' ou 'cuda' (auto-détecte CUDA si disponible)
DEVICE=cpu

# ============================================
# FILE UPLOAD CONFIGURATION
# ============================================

# Taille maximale des fichiers en bytes
# Par défaut: 52428800 (50 MB)
MAX_FILE_SIZE=52428800

# Répertoire de stockage des images téléchargées
UPLOAD_DIR=./uploads

# ============================================
# CORS CONFIGURATION
# ============================================

# Origines autorisées (séparées par des virgules)
# Format: protocol://host:port
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# ============================================
# LOGGING CONFIGURATION
# ============================================

# Niveau de log
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
